<h2>Напишите нам</h2>

<form action="/contacts" method="POST">

	<div class="form-group">
      <label>Ваша почта</label>
      <input name="contact[email]" type="text" class="form-control" placeholder="Введите ваш email" value="<%= @contact.email %>">
    </div>

    <div class="form-group">
      <label>Ваше сообщение</label>
      <textarea name="contact[message]" class="form-control" rows="3" placeholder="Ваше сообщение"><%= @contact.message %></textarea>
    </div>

    <button type="submit" class="btn btn-primary">Отправить</button>

</form>